#include <stdio.h>
/**
 * main : this is the entry point
 * return 0 : success
 */

int main(void)
{
	char c;

	c = '0';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
